%h2 Login

= error_messages_for :login

= form_for(@login, :action => url(:login) ) do
  %table
    %tr
      %th
        Login
      %td
        = text_field :login
    %tr
      %th
        Password
      %td
        = password_field :password, :label => :Password
  = submit "Login"

